const { resolve } = require('path')

module.exports = {
    config: resolve(__dirname,'src', 'config', 'database.js'), // caminho do arquivo de configuração da database
    'models-path': resolve(__dirname, 'src', 'app', 'models'), // caminho para a pasta de model
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),
    'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'),
}
